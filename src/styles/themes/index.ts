import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#003366' 
    },
    secondary: {
      main: '#efefef'
    },
    info: {
      main: '#ffffff',
      dark: '#efefef',
      contrastText: '#003366'
    }
  }
});


